import { TestBed } from '@angular/core/testing';
import { Store, StoreModule } from '@ngrx/store';
import { TRACKS_PER_PAGE } from 'src/constants';
import { testUtils } from 'src/utils/test';
import { TracklistRecord } from '../models/tracklist';
import { TracklistActions } from '../tracklist-actions';


describe('tracklists', () => {
  describe('selectors', () => {

    describe('getCurrentTracklist() selector', () => {});


    describe('getTracks() selector', () => {});

    describe('getTracksForCurrentTracklist() selector', () => {

    });


  });
});